# Pinned dependencies for the test suite + linting (CI uses this file).
#
# This is the lightweight, fully-offline subset needed to run `ruff` and
# `pytest`.  It excludes the heavy training stack (torch, prophet, xgboost,
# lightgbm, yfinance, matplotlib, seaborn) required only for the full
# `python main.py` benchmark — those live in requirements.txt.
#
# Versions are pinned to the exact set the suite was verified against.

numpy==2.5.0
pandas==3.0.3
PyYAML==6.0.3
scikit-learn==1.9.0
statsmodels==0.14.6
pytest==9.1.1
ruff==0.15.20
